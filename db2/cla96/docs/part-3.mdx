---
title: Part 3 · Security, access control & concurrency
description: Learn Db2 12.1 locking, isolation, authorities, privileges, RCAC, LBAC, trusted contexts, encryption and audit-oriented operations.
sidebar_position: 4
keywords: [Db2 locking, isolation levels, SECADM, DBADM, RCAC, LBAC, trusted context, Db2 security]
---

import {CourseProgress, Db2Terminal, KnowledgeCheck, LessonComplete, PracticeChecklist, ScenarioDecision} from '@site/src/components/LearningWidgets';

# Part 3: security, access control & concurrency

**Recommended time:** 8 hours

Security and concurrency share a DBA requirement: understand **who can do what, against which data, under which transaction semantics, with what evidence**.

<CourseProgress compact />

## Unit 1 · Concurrency is correctness under simultaneous work

Multiple applications can read and change the same data at the same time. Db2 protects correctness through mechanisms that include locking and versioning behavior governed by transaction isolation and access patterns.

The DBA’s job is not to “remove locks.” The job is to understand:

- what transaction holds the resource;
- what another transaction requests;
- lock mode and object scope;
- transaction duration and commit behavior;
- isolation level;
- access path and number of rows/pages touched;
- whether escalation, timeout or deadlock behavior is involved.

### Lock symptoms are not all the same

**Lock wait** — one unit of work waits for another to release an incompatible lock.

**Lock timeout** — a wait exceeds the configured timeout and the waiting operation is ended.

**Deadlock** — two or more units of work form a dependency cycle; Db2 chooses a victim to break the cycle.

**Lock escalation** — many lower-level locks are replaced by a higher-level lock to control lock-list usage, potentially increasing contention.

<Db2Terminal suggestions={['db2pd -db SAMPLE -locks show detail', 'db2pd -db SAMPLE -applications', 'db2 "values current isolation"']} />

<ScenarioDecision
  title="Latency rises while CPU remains normal"
  prompt="Application latency spikes. CPU and I/O remain close to baseline, while lock waits increase sharply. What should you do first?"
  options={[
    {label: 'Increase CPU capacity', correct: false, feedback: 'CPU is not the constrained signal. Capacity changes without a causal mechanism add cost but not evidence.'},
    {label: 'Identify the blocking chain, root blocker, lock modes, object and transaction context', correct: true, feedback: 'Lock-wait evidence points directly to contention. Root-blocker analysis establishes the causal transaction rather than treating symptoms.'},
    {label: 'Disable locking', correct: false, feedback: 'Locking is part of transaction correctness. The solution is workload/concurrency design, not removing protection.'},
    {label: 'Run a full database restore', correct: false, feedback: 'Recovery is unrelated to a normal contention incident and would cause major unnecessary impact.'},
  ]}
/>

## Unit 2 · Isolation levels: visibility vs concurrency

Db2 isolation levels commonly discussed in the CLA96G scope are:

| Isolation | General intent | Typical trade-off |
|---|---|---|
| UR — Uncommitted Read | Maximum read concurrency where dirty-read semantics are acceptable | Can observe uncommitted data |
| CS — Cursor Stability | Common transactional balance; protects the current row appropriately | Re-reading can observe change depending on access pattern |
| RS — Read Stability | Rows that qualify during the unit of work are protected from change | More locks / lower concurrency than CS |
| RR — Repeatable Read | Strong protection across the qualifying range/set | Highest concurrency cost among these levels |

The correct level is an **application correctness decision**. A DBA should not lower isolation simply to make lock waits disappear.

<KnowledgeCheck
  id="p3-isolation"
  question="What is the main trade-off when moving a workload toward stricter isolation?"
  options={[
    'Stricter isolation changes operating-system permissions',
    'Stricter isolation can strengthen consistency guarantees while increasing locking/concurrency cost',
    'Stricter isolation always improves throughput',
    'Isolation affects backup format only',
  ]}
  correctIndex={1}
  explanation="Isolation controls what concurrent transactions can observe and modify. Stronger guarantees can hold more protection for longer and therefore reduce concurrency."
/>

## Unit 3 · A lock-triage runbook

Use a repeatable sequence:

<div className="command-sequence">
  <div><strong>01 · Confirm symptom</strong><span>Wait time, timeout/deadlock messages, affected service and start time.</span></div>
  <div><strong>02 · Find blockers</strong><span>Identify waiting and holding application handles plus the locked object.</span></div>
  <div><strong>03 · Capture context</strong><span>SQL, transaction age, isolation, application identity and recent changes.</span></div>
  <div><strong>04 · Remediate safely</strong><span>Prefer application/transaction correction; force/terminate only with understood business impact.</span></div>
  <div><strong>05 · Prevent recurrence</strong><span>Review commit scope, indexing/access path, batch overlap, timeout and workload design.</span></div>
</div>

A forced application can roll back work and affect user transactions. Treat it as an operational change with impact, not a diagnostic click.

## Unit 4 · Authentication vs authorization

**Authentication** establishes identity. Depending on platform and configuration, identity may be validated through operating-system or external mechanisms.

**Authorization** determines what an authenticated identity can do inside Db2.

Do not conflate these failure domains:

- “password rejected / identity not established” is an authentication path;
- “SQL0551N / not authorized” is an authorization path;
- successful connection but denied object access is not a network problem.

### Least privilege and separation of duties

Avoid using broad administrative authority as a convenience mechanism. Build access from roles and task requirements.

A practical authorization review asks:

1. Which identity or role is the subject?
2. Which operation is required?
3. Which database/schema/object is in scope?
4. Which authority or privilege is the minimum sufficient grant?
5. Who is allowed to grant it?
6. Is the grant temporary or permanent?
7. How will it be reviewed and revoked?

## Unit 5 · Administrative authorities

The current public CLA96G objectives explicitly call out task authorities including **SECADM, DBADM, ACCESSCTRL and DATAACCESS**.

### SECADM

Security administration authority is intentionally powerful and separated from ordinary database administration. Use it for security policy and authority administration tasks that should not be casually mixed with application operations.

### DBADM

Database administration authority provides broad administrative capability for a database, but does not mean “security superuser.” Current Db2 documentation defines boundaries and exceptions; validate them for your service level rather than assuming DBADM can perform every security task.

### ACCESSCTRL

Access-control authority supports managing grants/revokes without automatically granting access to all application data.

### DATAACCESS

Data-access authority provides broad access to user data. It is operationally sensitive and should not be used as a substitute for proper application roles.

:::tip Design roles around duties
Examples: `APP_DEPLOYER`, `REPORT_READER`, `DBA_OPERATOR`, `PERF_ANALYST`. Role names should describe responsibility, not a person.
:::

<ScenarioDecision
  title="Support team requests DBADM"
  prompt="A support team needs to create and maintain objects in one application schema and execute an approved maintenance procedure. They request DBADM to reduce tickets. What is the strongest design?"
  options={[
    {label: 'Grant DBADM permanently', correct: false, feedback: 'Broad authority expands blast radius far beyond the described duty and weakens separation of duties.'},
    {label: 'Create a role with the required schema/object/procedure privileges and document any time-limited exception separately', correct: true, feedback: 'Role-based least privilege maps access to the task and creates a cleaner review/revocation path.'},
    {label: 'Grant DATAACCESS to PUBLIC', correct: false, feedback: 'That would expose broad data access to every user and is incompatible with least privilege.'},
    {label: 'Share the instance owner password', correct: false, feedback: 'Shared privileged credentials destroy attribution and create severe security risk.'},
  ]}
/>

## Unit 6 · Roles and object privileges

Prefer roles when several users or service identities perform the same controlled function.

Illustrative pattern:

```sql
CREATE ROLE REPORT_READER;
GRANT SELECT ON TABLE APP.SALES_SUMMARY TO ROLE REPORT_READER;
GRANT ROLE REPORT_READER TO USER ANALYST1;
```

The exact privilege model depends on object ownership, implicit privileges, authorities and group/role paths. During an authorization incident, inspect the **effective path**, not only direct grants.

### Review questions for a privilege grant

- Can the task be done with object privilege instead of database authority?
- Can a role replace repeated user-specific grants?
- Does the role cross environments or tenants unexpectedly?
- Is grant option required? Usually it should be avoided unless delegation is intentional.
- Is there a review/expiry process?

## Unit 7 · RCAC: row permissions and column masks

Row and Column Access Control (RCAC) enables fine-grained policy enforcement inside Db2.

**Row permissions** can restrict which rows are visible/operable for a user or session context.

**Column masks** can transform or hide sensitive column values under policy.

RCAC is powerful because the policy remains close to the data, but that also means testing must include:

- privileged and non-privileged identities;
- application service accounts;
- ad-hoc SQL/reporting users;
- maintenance and ETL processes;
- performance impact on representative queries;
- policy change and rollback procedures.

<KnowledgeCheck
  id="p3-rcac"
  question="Which capability most directly fits a requirement where users should see different rows from the same base table according to policy?"
  options={['RCAC row permissions', 'REORGCHK', 'LOGSECOND', 'HADR takeover']}
  correctIndex={0}
  explanation="RCAC supports database-enforced row permissions and column masks for fine-grained access control."
/>

## Unit 8 · LBAC and policy choice

Label-Based Access Control (LBAC) applies security labels and authorizations to protect data according to a label model. It can suit classification-style policies where data and users carry defined security labels.

Choose between RCAC, LBAC and ordinary privileges based on the policy model. Do not stack controls simply because they exist; every layer adds governance, testing and troubleshooting complexity.

A design comparison should consider:

| Requirement | Likely control family |
|---|---|
| Broad role/object access | Authorities, roles and privileges |
| Filter rows by session/user/business rule | RCAC row permissions |
| Mask sensitive column values | RCAC column masks |
| Classification/label policy | LBAC |
| Controlled identity switching in tiered applications | Trusted context |

## Unit 9 · Trusted contexts for multi-tier applications

In a three-tier architecture, a middleware connection may need to act on behalf of end users without creating an unmanaged pool of fully privileged credentials.

A trusted context can define conditions under which trusted connections and controlled user switching are permitted.

Evaluate:

- trusted system/network attributes;
- service identity ownership;
- which users may be switched to;
- which roles become available;
- auditing requirements;
- failure behavior when trust conditions are not met.

Do not use a trusted context as a way to hide user attribution. Its purpose is controlled delegation, not anonymity.

## Unit 10 · Encryption in transit and at rest

Security architecture should distinguish:

- **data in transit** — protect client/server connections with supported TLS configurations and certificate governance;
- **data at rest** — database/storage encryption controls, key management and backup protection;
- **credentials/secrets** — store outside scripts and source repositories; rotate according to policy.

A “TLS enabled” checkbox is not a complete control. Review protocol versions, certificate validity, hostname verification, trust stores, key ownership and renewal automation.

## Unit 11 · Auditing and evidence

Audit design answers **which events must be attributable and reviewable**. Examples include:

- privileged security changes;
- grant/revoke activity;
- access to regulated tables;
- authentication failures;
- administrative actions;
- policy changes.

Audit volume can be significant. Define retention, protected storage, review ownership, alerting and access controls around the audit trail itself.

### Security incident evidence bundle

```text
Identity / auth ID:
Connection origin:
Database / schema / object:
Requested operation:
Error / SQLCODE:
Direct grants:
Role/group path:
Relevant authority:
RCAC/LBAC/trusted-context policy:
Recent security changes:
Audit evidence:
Resolution / rollback:
```

<PracticeChecklist
  title="Part 3 lab evidence"
  items={[
    'Observed or simulated a lock wait and identified holder, waiter, object and transaction context.',
    'Compared UR, CS, RS and RR in terms of visibility and concurrency trade-offs.',
    'Mapped a sample support duty to least-privilege role/object grants rather than broad authority.',
    'Explained the separation among SECADM, DBADM, ACCESSCTRL and DATAACCESS.',
    'Designed a row-level policy use case suitable for RCAC.',
    'Compared RCAC and LBAC for two different policy models.',
    'Documented the trust conditions that would be required for a three-tier trusted context.',
    'Created a security evidence checklist covering identity, effective authorization path and audit evidence.',
  ]}
/>

<KnowledgeCheck
  id="p3-auth"
  question="An identity connects successfully but receives an authorization error when selecting from APP.PAYROLL. Which failure domain should you investigate first?"
  options={['Network routing', 'Authentication only', 'Effective authorization: authorities, roles/groups, privileges and data policies', 'Backup history']}
  correctIndex={2}
  explanation="The successful connection establishes that the identity reached Db2. The denied data operation points to the effective authorization and policy path."
/>

## Part 3 review questions

1. What is the difference between a lock wait, timeout, deadlock and escalation?
2. How do UR, CS, RS and RR change correctness/concurrency trade-offs?
3. Why is “force the blocker” not a complete lock-resolution strategy?
4. What is the difference between authentication and authorization?
5. How do SECADM, DBADM, ACCESSCTRL and DATAACCESS serve different duties?
6. When is a role better than direct user grants?
7. What policy problems fit RCAC vs LBAC?
8. Why are trusted contexts useful in multi-tier applications?
9. What must be governed in addition to simply enabling TLS?
10. Which audit events matter for your organization’s highest-risk database actions?

### Authoritative references

- Db2 12.1 documentation: `https://www.ibm.com/docs/en/db2/12.1`
- Current Db2 DBADM authority reference: `https://www.ibm.com/docs/en/db2/11.5.x?topic=authorities-database-administration-authority-dbadm`
- CLA93G public security/concurrency objectives: `https://www.ibm.com/training/course/ibm-db2-121-security-and-concurrency-for-relational-dbas-CLA93G`

<LessonComplete lessonId="part-3" nextHref="/course/part-4" nextLabel="Start Part 4" />
