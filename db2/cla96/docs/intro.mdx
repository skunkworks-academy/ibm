---
title: Orientation · How to use this course
description: Prepare your Db2 12.1 practice environment, learning evidence, and DBA operating model before starting CLA96G.
sidebar_position: 1
keywords: [CLA96G, Db2 12.1, DBA training, relational database administration]
---

import {CourseProgress, Db2Terminal, KnowledgeCheck, LessonComplete, PracticeChecklist, ScenarioDecision} from '@site/src/components/LearningWidgets';

# Orientation: learn as a production DBA

This self-paced learning companion turns the public CLA96G skill scope into an **original, practice-led DBA pathway**. The target is not command memorisation. The target is repeatable operational judgment: understand the layer you are changing, collect evidence, make the smallest justified intervention, validate the result, and leave an audit trail.

<CourseProgress compact />

:::info Course alignment
IBM currently describes CLA96G as **IBM Db2 12: Foundation for Relational DBAs**, focused on Db2 12.1 administration, utilities, recovery, security, concurrency, monitoring and performance. This independent companion aligns to those public objectives without reproducing IBM courseware.
:::

## What you should be able to do at the end

By completion, you should be able to:

- explain the relationship between the operating system, Db2 instance, database, schemas and database objects;
- identify the exact Db2 service level and reason about maintenance scope before changing an environment;
- inspect and change database configuration using a baseline/change/validation workflow;
- operate data movement and maintenance utilities while monitoring their state and table implications;
- design and test backup, restore and rollforward procedures against recovery objectives;
- diagnose locks, timeouts, deadlocks and isolation-related concurrency behavior;
- separate authentication, authorization, task authorities, roles and object privileges;
- reason about RCAC, LBAC, trusted contexts, auditing and encryption as layered controls;
- use RUNSTATS, indexes, EXPLAIN and monitoring data to build a performance hypothesis;
- evaluate advisor and AI-assisted recommendations without bypassing change governance.

## Recommended learning environment

A real lab is strongly recommended. The browser terminal in this course is intentionally a simulator and does not connect to Db2.

<div className="callout-grid">
  <div className="callout-card"><strong>Db2 environment</strong><p>An isolated Db2 12.1 lab or an organization-approved sandbox. Never test destructive recovery or privilege changes on production first.</p></div>
  <div className="callout-card"><strong>DBA notebook</strong><p>Record timestamps, commands, outputs, screenshots, hypotheses, decisions and rollback points. Treat evidence as part of the exercise.</p></div>
  <div className="callout-card"><strong>Sample workload</strong><p>Use a database such as SAMPLE or a disposable training database with enough data to observe utility, locking and plan behavior.</p></div>
</div>

<PracticeChecklist
  title="Environment readiness"
  items={[
    'I can identify the Db2 host, instance owner and database I am authorized to use.',
    'I have a disposable or recoverable practice database.',
    'I know where Db2 diagnostic and utility messages are written in my environment.',
    'I have a folder or notebook for command output and screenshots.',
    'I have confirmed that production changes are outside the scope of these exercises unless separately authorized.',
  ]}
/>

## The five-step operating discipline

<div className="command-sequence">
  <div><strong>01 · Baseline</strong><span>Capture service level, configuration, workload rate, health and symptoms before intervention.</span></div>
  <div><strong>02 · Hypothesis</strong><span>Name the mechanism you believe explains the symptom and what evidence would support or falsify it.</span></div>
  <div><strong>03 · Change</strong><span>Choose the smallest scoped change that tests the hypothesis and preserve rollback values.</span></div>
  <div><strong>04 · Validate</strong><span>Compare post-change technical signals and user-facing outcomes with the baseline.</span></div>
  <div><strong>05 · Document</strong><span>Record what changed, why, who approved it, evidence, outcome and follow-up.</span></div>
</div>

This sequence will appear repeatedly. It is deliberately more important than any individual command.

## Start with service-level evidence

Before a DBA can reason about fixes, deprecated behavior, compatibility or a support case, the exact installed level matters.

<Db2Terminal suggestions={['db2level', 'db2 get db cfg for SAMPLE', 'db2 list utilities show detail']} />

In your real lab, capture `db2level` output. Do not reduce the result to “Db2 12.1”; keep the detailed identifiers needed for maintenance planning.

## Exercise: write your first evidence note

Use this structure:

```text
Timestamp:
Environment / host:
Instance:
Database:
Observed symptom or learning objective:
Baseline commands and outputs:
Hypothesis:
Change (if any):
Validation evidence:
Rollback state:
Follow-up:
```

A strong DBA note allows another operator to understand what happened without relying on memory.

<ScenarioDecision
  title="Intermittent connection failure after an OS patch"
  prompt="Several databases in the same Db2 instance intermittently reject connections after a server patch. Which investigation gives the highest-value first evidence?"
  options={[
    {label: 'Restore one database immediately', correct: false, feedback: 'A symptom shared by several databases points above a single database first. Restoring creates major risk without evidence of corruption.'},
    {label: 'Check instance state, listener/service port, OS/network changes and time-correlated diagnostics', correct: true, feedback: 'The shared failure domain suggests instance, service, operating-system or network scope. Establish that layer before database-level repair.'},
    {label: 'Run REORG on all application tables', correct: false, feedback: 'Table organization is not a plausible first cause for connection refusal across several databases.'},
    {label: 'Increase the application connection pool', correct: false, feedback: 'Pool changes may mask demand but do not explain why the server is intermittently refusing connections.'},
  ]}
/>

<KnowledgeCheck
  id="intro-scope"
  question="When the same connection symptom affects multiple databases in one instance, which scope should you investigate early?"
  options={['One random table', 'Instance/service/OS/network scope', 'Index clustering only', 'RUNSTATS profiles only']}
  correctIndex={1}
  explanation="Shared symptoms across databases make a shared runtime or infrastructure layer a higher-probability failure domain than one database object."
/>

## How progress and assessment work

- Each major milestone has a **Mark complete** control.
- Progress is stored in your browser; no identity or personal data is required.
- If the content is launched from a SCORM 1.2 LMS, the course attempts to report lesson status and final score.
- The final assessment uses 16 scenario-based questions and a **75% mastery threshold**.
- The completion record is evidence of this companion’s assessment only; it is not an IBM certification or an official IBM course completion record.

## Reference discipline

Prefer vendor documentation for exact command syntax, parameter semantics, supported configurations and service-level behavior. The course intentionally teaches a decision model and then points you back to authoritative reference material.

Useful starting points:

- IBM Db2 12.1 documentation: `https://www.ibm.com/docs/en/db2/12.1`
- IBM CLA96G course page: `https://www.ibm.com/training/course/ibm-db2-12-foundation-for-relational-dbas-CLA96G`
- IBM Db2 tutorials: `https://www.ibm.com/products/db2/tutorials`

<LessonComplete lessonId="intro" nextHref="/course/part-1" nextLabel="Start Part 1" />
